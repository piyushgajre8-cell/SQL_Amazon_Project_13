# SQL_Amazon_Project_13
This SQL file creates a database named pet_store_db with a pets table. It stores details like pet ID, name, and type. It adds sample data for dogs, cats, fish, and parrots, then shows how to delete a specific record and remove all data using the TRUNCATE command.
